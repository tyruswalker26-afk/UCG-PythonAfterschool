# Import a library that can transform normal text into stylized ASCII art 
# Hint the library name is: pyfiglet

import pyfiglet 
# Ask the user to provide some text (think about what type of input this returns)
#Hint: create a variable called user_input 
user_input=input("type any number or letter: ")


# Use a function from the library to convert the text into ASCII art
# Hint: Create a variable called ascii_art that stores and converts user input to ascii art pyfiglet.figlet_format(user_input)


# Display the transformed ASCII art in the console
# Hint: print the variable ascii_art
